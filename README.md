# status

Status page for TarkovTracker.org services.

## Run locally

```bash
npm install
npm run start
```

Open `http://localhost:3000`.

## Configure monitored services

Edit `services.json` to point each service at the correct health endpoint. The UI updates automatically every 30 seconds.
